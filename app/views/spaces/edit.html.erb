<section class="max-w-2xl mx-auto p-4 bg-neutral rounded-lg shadow-md">
  <h1 class="font-bold text-4xl mb-6">Editing space</h1>

  <%= render "form", space: @space, tenant: @tenant %>

  <div class="mt-6 space-x-2">
    <%= link_to "Show this space", [@tenant, @space], class: "btn btn-secondary" %>
    <%= link_to "Back to spaces", tenant_spaces_path, class: "btn btn-secondary" %>
  </div>
</section>
